---
description: 仅同步 iGPSPORT 户外骑行，禁止其它运动类型与室内骑行
alwaysApply: true
---

# 范围紧箍咒

本应用只把 **iGPSPORT 码表的户外骑行 FIT** 写入本机 Apple 健康。设备环境固定为开发者自用：iPhone 17 Pro Max / iOS 27。

## 允许

- FIT `sport == 2`（骑行），且不是室内（`sub_sport != 6`）
- 骑行距离、真实骑行时间、总耗时、GPS 路线、速度、圈段、爬升，以及该文件里实际存在的心率 / 踏频 / 功率
- 保持单页、无账号、无去重、无冗余交互

## 禁止

- 不要为跑步、步行、徒步、游泳、室内骑行或「其它运动」增加解析分支、HealthKit 类型映射或 UI
- 不要为其它品牌、其它系统版本或其它机型做兼容
- 不要把室内骑行标成户外，也不要把户外骑行写成通用运动
- 文件不是户外骑行时直接拒绝，不要部分导入

后继需求若超出上述范围，先改本规则和 README，再改代码。
